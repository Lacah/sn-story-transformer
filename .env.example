# .env.example — copy to .env and fill in your values (never commit .env)
#
# Two scripts use this file:
#   analyze_doc.py    — requires AI configuration (below)
#   create_stories.py — requires ServiceNow configuration (below)


# ── ServiceNow — required for create_stories.py ───────────────────────────

SERVICENOW_INSTANCE=https://your-instance.service-now.com
SERVICENOW_PRODUCT_SYS_ID=your_product_sys_id_here

# Authentication method: oauth (default) or basic
SERVICENOW_AUTH=oauth

# OAuth credentials (when SERVICENOW_AUTH=oauth)
SERVICENOW_CLIENT_ID=your_oauth_client_id_here
SERVICENOW_CLIENT_SECRET=your_oauth_client_secret_here

# Basic auth credentials (when SERVICENOW_AUTH=basic)
# SERVICENOW_USERNAME=your_username_here
# SERVICENOW_PASSWORD=your_password_here


# ── AI model — required for analyze_doc.py ────────────────────────────────
#
# Set AI_MODEL to your provider's model string, then uncomment the matching
# API key block below. If AI_MODEL is not set, the script auto-detects based
# on whichever API key is present.
#
# Provider          AI_MODEL value                   API key variable
# ──────────────    ──────────────────────────────   ─────────────────────────
# Anthropic Claude  claude-sonnet-4-6                ANTHROPIC_API_KEY
# OpenAI GPT        gpt-4o                           OPENAI_API_KEY
# Google Gemini     gemini/gemini-1.5-pro            GEMINI_API_KEY
# Azure OpenAI      azure/<your-deployment-name>     AZURE_API_KEY + AZURE_API_BASE
# Mistral           mistral/mistral-large-latest     MISTRAL_API_KEY
# Cohere            command-r-plus                   COHERE_API_KEY
#
# Full list of supported providers and model strings:
#   https://docs.litellm.ai/docs/providers

# AI_MODEL=claude-sonnet-4-6
# ANTHROPIC_API_KEY=sk-ant-...

# AI_MODEL=gpt-4o
# OPENAI_API_KEY=sk-...

# AI_MODEL=gemini/gemini-1.5-pro
# GEMINI_API_KEY=...
